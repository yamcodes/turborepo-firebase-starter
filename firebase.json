{
  "functions": [
    {
      "source": "apps/api",
      "predeploy": [
        "pnpm --filter \"./$RESOURCE_DIR\" lint",
        "pnpm --filter \"./$RESOURCE_DIR\" build"
      ],
      "codebase": "default",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log"
      ]
    }
  ],
  "hosting": {
    "public": "apps/web/dist",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [
      {
        "source": "/api/**",
        "function": "server"
      },
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  },
  "emulators": {
    "ui": {
      "enabled": true
    },
    "functions": {
      "port": 3001
    },
    "hosting": {
      "enabled": true,
      "port": 3000
    }
  }
}
